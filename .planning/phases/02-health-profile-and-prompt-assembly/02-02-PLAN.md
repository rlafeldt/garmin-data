---
phase: 02-health-profile-and-prompt-assembly
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - src/biointelligence/prompt/__init__.py
  - src/biointelligence/prompt/models.py
  - src/biointelligence/prompt/assembler.py
  - src/biointelligence/prompt/templates.py
  - src/biointelligence/prompt/budget.py
  - tests/test_prompt.py
autonomous: true
requirements: [PROF-02, TRND-04]

must_haves:
  truths:
    - "The assembled prompt contains today's metrics, 7-day rolling context, health profile, and sports science grounding in structured XML-tagged sections"
    - "Health profile content appears in the assembled prompt (PROF-02: injected into every analysis call)"
    - "Sports science grounding blocks are embedded in the prompt (TRND-04: periodization, HRV interpretation, sleep architecture, ACWR)"
    - "The prompt stays within a defined token budget (~4K-6K tokens)"
    - "Trimming follows priority order: grounding first, then activities, then trends (metrics and profile never cut)"
    - "Yesterday's activities are included as a dedicated prompt section"
    - "Output format is specified as a JSON schema defining the DailyProtocol structure"
    - "Each analysis domain gets specific directive instructions"
  artifacts:
    - path: "src/biointelligence/prompt/models.py"
      provides: "PromptContext and AssembledPrompt models, preliminary DailyProtocol schema"
      exports: ["PromptContext", "AssembledPrompt", "DailyProtocol"]
    - path: "src/biointelligence/prompt/assembler.py"
      provides: "Main prompt assembly function"
      exports: ["assemble_prompt"]
    - path: "src/biointelligence/prompt/templates.py"
      provides: "Sports science grounding blocks and analysis directive templates"
      exports: ["SPORTS_SCIENCE_GROUNDING", "ANALYSIS_DIRECTIVES"]
    - path: "src/biointelligence/prompt/budget.py"
      provides: "Token estimation and trimming logic"
      exports: ["estimate_tokens", "trim_to_budget"]
    - path: "tests/test_prompt.py"
      provides: "Prompt assembly, structure, budget, and trimming tests"
  key_links:
    - from: "src/biointelligence/prompt/assembler.py"
      to: "src/biointelligence/profile/models.py"
      via: "HealthProfile.model_dump() for prompt serialization"
      pattern: "HealthProfile"
    - from: "src/biointelligence/prompt/assembler.py"
      to: "src/biointelligence/trends/models.py"
      via: "TrendResult for 7-day rolling context"
      pattern: "TrendResult"
    - from: "src/biointelligence/prompt/assembler.py"
      to: "src/biointelligence/garmin/models.py"
      via: "DailyMetrics for today's data, Activity for yesterday's activities"
      pattern: "DailyMetrics|Activity"
    - from: "src/biointelligence/prompt/assembler.py"
      to: "src/biointelligence/prompt/budget.py"
      via: "estimate_tokens + trim_to_budget for budget enforcement"
      pattern: "estimate_tokens|trim_to_budget"
---

<objective>
Assemble a structured Claude prompt from health profile, today's metrics, 7-day trends, yesterday's activities, sports science grounding, and analysis directives -- with XML-tagged sections, a DailyProtocol JSON output schema, and token budget enforcement.

Purpose: This is the final output of Phase 2 -- a complete prompt string ready for Phase 3's Claude API call. It connects all data sources into a single structured document that tells Claude exactly what to analyze and how to respond.

Output: Prompt assembler, sports science grounding templates, token budget management, DailyProtocol output schema, comprehensive tests.
</objective>

<execution_context>
@/Users/raphaellafeldt/.claude/get-shit-done/workflows/execute-plan.md
@/Users/raphaellafeldt/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-health-profile-and-prompt-assembly/02-CONTEXT.md
@.planning/phases/02-health-profile-and-prompt-assembly/02-RESEARCH.md
@.planning/phases/02-health-profile-and-prompt-assembly/02-01-SUMMARY.md

<interfaces>
<!-- Types from Plan 01 that this plan consumes directly. -->

From src/biointelligence/profile/models.py (created in Plan 01):
```python
class HealthProfile(BaseModel):
    biometrics: Biometrics
    training: TrainingContext
    medical: MedicalHistory
    metabolic: MetabolicProfile
    diet: DietPreferences
    supplements: list[Supplement]
    sleep_context: SleepContext
    lab_values: dict[str, LabValue] = {}
```

From src/biointelligence/trends/models.py (created in Plan 01):
```python
class TrendDirection(str, Enum):
    IMPROVING = "improving"
    DECLINING = "declining"
    STABLE = "stable"
    INSUFFICIENT = "insufficient_data"

class MetricTrend(BaseModel):
    avg: float | None = None
    min_val: float | None = None
    max_val: float | None = None
    direction: TrendDirection = TrendDirection.INSUFFICIENT

class TrendResult(BaseModel):
    window_start: date
    window_end: date
    data_points: int
    metrics: dict[str, MetricTrend]
```

From src/biointelligence/garmin/models.py (Phase 1):
```python
class DailyMetrics(BaseModel):
    date: DateType
    # 25+ metric fields (all Optional except date)
    # See full definition in Plan 01 interfaces

class Activity(BaseModel):
    date: DateType
    activity_type: str
    name: str | None = None
    duration_seconds: int | None = None
    avg_hr: int | None = None
    max_hr: int | None = None
    calories: int | None = None
    training_effect_aerobic: float | None = None
    training_effect_anaerobic: float | None = None
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Prompt models, sports science templates, and token budget management</name>
  <files>
    src/biointelligence/prompt/__init__.py,
    src/biointelligence/prompt/models.py,
    src/biointelligence/prompt/templates.py,
    src/biointelligence/prompt/budget.py
  </files>
  <behavior>
    - Test: estimate_tokens returns ~len(text) // 4 for various text lengths
    - Test: estimate_tokens handles empty string (returns 0)
    - Test: trim_to_budget with sections under budget returns all sections unchanged
    - Test: trim_to_budget removes lowest-priority section first when over budget
    - Test: trim_to_budget follows priority order: sports_science trimmed first, then yesterday_activities, then trends_7d
    - Test: trim_to_budget never removes today_metrics or health_profile sections
    - Test: DailyProtocol model validates with all 5 domains populated
    - Test: DailyProtocol.model_json_schema() produces valid JSON schema string
    - Test: SPORTS_SCIENCE_GROUNDING contains HRV interpretation, sleep architecture, ACWR, and periodization blocks
    - Test: ANALYSIS_DIRECTIVES contains sections for training, recovery, sleep, nutrition, supplementation
  </behavior>
  <action>
    1. Create `src/biointelligence/prompt/__init__.py` exporting PromptContext, AssembledPrompt, assemble_prompt, DailyProtocol.

    2. Create `src/biointelligence/prompt/models.py`:
       - PromptContext(BaseModel): today_metrics: DailyMetrics, trends: TrendResult, profile: HealthProfile, activities: list[Activity], target_date: date
       - AssembledPrompt(BaseModel): text: str, estimated_tokens: int, sections_included: list[str], sections_trimmed: list[str]
       - DailyProtocol(BaseModel): Preliminary output schema with 5 domain sections. Each domain has a Pydantic model:
         - TrainingRecommendation: readiness_score (int, 1-10), readiness_summary (str), recommended_intensity (str), recommended_type (str), recommended_duration_minutes (int), training_load_assessment (str), reasoning (str)
         - RecoveryAssessment: recovery_status (str), hrv_interpretation (str), body_battery_assessment (str), stress_impact (str), recommendations (list[str]), reasoning (str)
         - SleepAnalysis: quality_assessment (str), architecture_notes (str), optimization_tips (list[str]), reasoning (str)
         - NutritionGuidance: caloric_target (str), macro_focus (str), hydration_target (str), meal_timing_notes (str), reasoning (str)
         - SupplementationPlan: adjustments (list[str]), timing_notes (str), reasoning (str)
         - DailyProtocol: date (str), training: TrainingRecommendation, recovery: RecoveryAssessment, sleep: SleepAnalysis, nutrition: NutritionGuidance, supplementation: SupplementationPlan, overall_summary (str), data_quality_notes (str | None)

       Note: This is a preliminary schema. Phase 3 may refine field names and add fields. The key purpose here is to provide Claude with a concrete output format spec in the prompt.

    3. Create `src/biointelligence/prompt/templates.py`:
       - SPORTS_SCIENCE_GROUNDING: str constant with 3-5 short framework reference blocks (3-5 sentences each):
         a) HRV interpretation model: what overnight HRV means, baseline vs acute changes, how to interpret status (balanced/sympathetic/parasympathetic)
         b) Sleep architecture guidelines: deep sleep targets by age, REM importance for cognitive recovery, minimum thresholds
         c) Acute-to-chronic workload ratio (ACWR): sweet spot 0.8-1.3, injury risk above 1.5, calculation from 7-day acute vs 28-day chronic
         d) Periodization principles: base/build/peak/recovery phases, volume before intensity, taper timing
       - ANALYSIS_DIRECTIVES: str constant with structured instructions for each of the 5 analysis domains. Each directive tells Claude exactly what to analyze and what to output. Format as clear sections.

    4. Create `src/biointelligence/prompt/budget.py`:
       - estimate_tokens(text: str) -> int: return len(text) // 4 (per user decision: ~1 token per 4 chars, no external tokenizer)
       - SECTION_PRIORITY: ordered list of section tag names from lowest to highest priority (trimmed first to last): ["sports_science", "yesterday_activities", "trends_7d", "analysis_directives", "output_format", "health_profile", "today_metrics"]. Per user decision, trimming order is: grounding cut first, then activities, then trends. The last two (health_profile, today_metrics) are NEVER trimmed.
       - NEVER_TRIM: set of section names that must not be removed = {"health_profile", "today_metrics"}
       - DEFAULT_TOKEN_BUDGET: int = 6000 (upper end of ~4K-6K range)
       - trim_to_budget(sections: dict[str, str], budget: int = DEFAULT_TOKEN_BUDGET) -> tuple[dict[str, str], list[str]]: Estimate total tokens. If under budget, return (sections, []). Otherwise, iterate SECTION_PRIORITY from lowest priority upward, removing sections until under budget. Never remove NEVER_TRIM sections. Return (remaining_sections, list of trimmed section names). Log warnings with structlog when trimming.
  </action>
  <verify>
    <automated>uv run pytest tests/test_prompt.py -x -v -k "not test_assemble"</automated>
  </verify>
  <done>
    - DailyProtocol Pydantic model defines the 5-domain output schema (training, recovery, sleep, nutrition, supplementation)
    - model_json_schema() produces valid JSON schema for prompt injection
    - Sports science grounding covers HRV, sleep architecture, ACWR, and periodization
    - Analysis directives cover all 5 domains with specific instructions
    - Token estimation uses 4-char heuristic
    - Trimming respects priority order and never removes metrics or profile
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prompt assembler wiring all data sources into XML-tagged prompt</name>
  <files>
    src/biointelligence/prompt/assembler.py,
    tests/test_prompt.py
  </files>
  <behavior>
    - Test: assemble_prompt produces AssembledPrompt with non-empty text and estimated_tokens > 0
    - Test: assembled prompt contains <health_profile> XML section with profile data
    - Test: assembled prompt contains <today_metrics> XML section with metric values
    - Test: assembled prompt contains <trends_7d> XML section with trend data
    - Test: assembled prompt contains <yesterday_activities> XML section with activity data
    - Test: assembled prompt contains <sports_science> XML section with grounding text
    - Test: assembled prompt contains <analysis_directives> XML section
    - Test: assembled prompt contains <output_format> XML section with DailyProtocol JSON schema
    - Test: assembled prompt estimated_tokens is within ~4K-6K range for typical input
    - Test: when assembled prompt exceeds budget, sections_trimmed lists removed sections
    - Test: assemble_prompt with empty activities list produces valid prompt (activities section says "No activities recorded")
    - Test: assemble_prompt with all-INSUFFICIENT trends produces valid prompt (trends section indicates insufficient data)
    - Test: health profile lab values with dates appear in prompt text
    - Test: supplement conditional dosing rules appear in prompt text
  </behavior>
  <action>
    1. Create `src/biointelligence/prompt/assembler.py`:
       - _format_metrics(metrics: DailyMetrics) -> str: Format today's metrics as human-readable key-value text. Group by category (Sleep, HRV, Body Battery, Heart Rate, Stress, Training, General). Skip None values. Convert total_sleep_seconds to hours:minutes for readability.
       - _format_trends(trends: TrendResult) -> str: Format each MetricTrend as "metric_name: avg=X, min=X, max=X, direction=X". Include window dates and data point count. If all metrics INSUFFICIENT, output "Insufficient data for trend analysis (fewer than 4 days of data available)."
       - _format_activities(activities: list[Activity]) -> str: Format each activity as "type: name | duration: Xmin | avg_hr: X | training_effect: X". If no activities, output "No activities recorded yesterday."
       - _format_profile(profile: HealthProfile) -> str: Serialize the health profile in a prompt-friendly text format. Include all sections. For lab values, include value + unit + date + range. For supplements, include name + dose + timing + condition. Use Pydantic model_dump(mode="json") for consistent serialization, then format as readable text (not raw JSON).
       - _format_output_schema() -> str: Use DailyProtocol.model_json_schema() to generate JSON schema string. Wrap in brief instructions telling Claude to return valid JSON matching this schema.
       - assemble_prompt(context: PromptContext, token_budget: int = 6000) -> AssembledPrompt:
         a) Build sections dict: {"health_profile": _format_profile(...), "today_metrics": _format_metrics(...), "trends_7d": _format_trends(...), "yesterday_activities": _format_activities(...), "sports_science": SPORTS_SCIENCE_GROUNDING, "analysis_directives": ANALYSIS_DIRECTIVES, "output_format": _format_output_schema()}
         b) Call trim_to_budget(sections, token_budget) -> get remaining sections and trimmed list
         c) Assemble final text: join remaining sections with XML tags: "<{tag}>\n{content}\n</{tag}>\n\n" for each section, ordered by: health_profile, today_metrics, trends_7d, yesterday_activities, sports_science, analysis_directives, output_format
         d) Compute estimated_tokens on final text
         e) Return AssembledPrompt(text=..., estimated_tokens=..., sections_included=..., sections_trimmed=...)
         f) Log with structlog: estimated tokens, sections included, sections trimmed

    2. Add prompt assembly tests to `tests/test_prompt.py`:
       - Create fixtures: mock HealthProfile, mock DailyMetrics, mock TrendResult, mock Activity list
       - Test all XML sections present using regex or string containment checks
       - Test token budget enforcement with artificially low budget (e.g., 500 tokens)
       - Test edge cases: empty activities, all-INSUFFICIENT trends
       - Test that lab values and supplement conditions appear in the prompt text

    3. Update `src/biointelligence/prompt/__init__.py` to also export assemble_prompt.
  </action>
  <verify>
    <automated>uv run pytest tests/test_prompt.py -x -v</automated>
  </verify>
  <done>
    - assemble_prompt produces a complete XML-tagged prompt with all 7 sections
    - Health profile is injected as structured context (PROF-02)
    - Sports science grounding is embedded (TRND-04)
    - Token budget is enforced with priority-based trimming
    - Yesterday's activities included with training effect data
    - DailyProtocol JSON schema specifies the output format
    - Edge cases handled: empty activities, insufficient trends
    - Full test suite passing including all Plan 01 tests
  </done>
</task>

</tasks>

<verification>
All Phase 2 Plan 02 checks:
1. `uv run pytest tests/test_prompt.py -x -v` -- all prompt tests pass
2. `uv run pytest tests/ -x -q` -- entire test suite passes (profile, trends, prompt, and Phase 1 tests)
3. `uv run ruff check src/biointelligence/prompt/` -- no lint errors
4. `python -c "from biointelligence.prompt import assemble_prompt, DailyProtocol; print('prompt OK')"` -- imports work
5. Assembled prompt for typical input is within ~4K-6K estimated tokens
</verification>

<success_criteria>
- assemble_prompt produces a complete structured prompt with XML-tagged sections: health_profile, today_metrics, trends_7d, yesterday_activities, sports_science, analysis_directives, output_format
- Health profile with lab values and supplement conditions appears in the prompt (PROF-02)
- Sports science grounding blocks cover HRV, sleep architecture, ACWR, periodization (TRND-04)
- Token budget enforced: prompt ~4K-6K tokens, trimming follows priority order
- DailyProtocol JSON schema defines 5-domain output (training, recovery, sleep, nutrition, supplementation)
- All new and existing tests pass
- The prompt is ready for Phase 3's Claude API call
</success_criteria>

<output>
After completion, create `.planning/phases/02-health-profile-and-prompt-assembly/02-02-SUMMARY.md`
</output>
