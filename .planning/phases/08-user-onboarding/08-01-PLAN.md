---
phase: 08-user-onboarding
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - onboarding/package.json
  - onboarding/tsconfig.json
  - onboarding/next.config.ts
  - onboarding/tailwind.config.ts
  - onboarding/src/app/layout.tsx
  - onboarding/src/app/page.tsx
  - onboarding/src/app/onboarding/layout.tsx
  - onboarding/src/lib/supabase.ts
  - onboarding/src/lib/types.ts
  - onboarding/src/lib/schemas/step-1.ts
  - onboarding/src/lib/schemas/step-2.ts
  - onboarding/src/lib/schemas/step-3.ts
  - onboarding/src/lib/schemas/step-4.ts
  - onboarding/src/lib/schemas/step-5.ts
  - onboarding/src/lib/schemas/step-6.ts
  - onboarding/src/lib/completeness.ts
  - onboarding/src/components/onboarding/step-progress.tsx
  - onboarding/src/components/onboarding/step-navigation.tsx
  - onboarding/.env.local.example
  - supabase/onboarding-ddl.sql
autonomous: true
requirements: [ONBD-01, ONBD-03]

must_haves:
  truths:
    - "Next.js project builds and starts with `npm run dev`"
    - "Supabase DDL creates onboarding_profiles, lab_results, and consent_records tables"
    - "Zod schemas define all fields from the 6-step questionnaire with correct types and validation"
    - "Progress indicator and navigation components render correctly"
    - "Supabase client connects using NEXT_PUBLIC_ env vars"
  artifacts:
    - path: "onboarding/package.json"
      provides: "Next.js project with all dependencies"
      contains: "react-hook-form"
    - path: "onboarding/src/lib/schemas/step-1.ts"
      provides: "Zod schema for biological profile with required/optional field distinction"
      exports: ["step1Schema", "Step1Data"]
    - path: "supabase/onboarding-ddl.sql"
      provides: "Database schema for onboarding tables"
      contains: "onboarding_profiles"
    - path: "onboarding/src/lib/supabase.ts"
      provides: "Supabase client singleton"
      exports: ["supabase"]
    - path: "onboarding/src/lib/completeness.ts"
      provides: "Profile completeness calculation"
      exports: ["calculateCompleteness"]
  key_links:
    - from: "onboarding/src/lib/schemas/*.ts"
      to: "supabase/onboarding-ddl.sql"
      via: "Schema fields must match JSONB column structure"
      pattern: "step_\\d+_data"
    - from: "onboarding/src/lib/supabase.ts"
      to: "NEXT_PUBLIC_SUPABASE_URL"
      via: "Environment variable"
      pattern: "NEXT_PUBLIC_SUPABASE"
---

<objective>
Scaffold the Next.js onboarding application, define Supabase DDL for all onboarding tables, create Zod validation schemas for all 6 wizard steps, and build shared components (progress indicator, step navigation).

Purpose: Establishes the foundation that all subsequent frontend plans build on -- project structure, data contracts (Zod schemas), database schema, and reusable UI primitives.
Output: Buildable Next.js project at `onboarding/`, Supabase DDL file, complete Zod schemas for all 6 steps, shared layout and navigation components.
</objective>

<execution_context>
@/Users/raphaellafeldt/.claude/get-shit-done/workflows/execute-plan.md
@/Users/raphaellafeldt/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-user-onboarding/08-CONTEXT.md
@.planning/phases/08-user-onboarding/08-RESEARCH.md

<interfaces>
<!-- Supabase client pattern from existing Python codebase (for schema alignment) -->
From src/biointelligence/config.py:
```python
supabase_url: str
supabase_key: str
```

From src/biointelligence/profile/models.py (fields the Zod schemas must cover):
```python
class Biometrics(BaseModel):
    age: int; sex: str; weight_kg: float; height_cm: float; body_fat_pct: float | None = None

class TrainingContext(BaseModel):
    phase: str; weekly_volume_hours: float; preferred_types: list[str]
    race_goals: list[RaceGoal] = []; injury_history: list[Injury] = []

class MedicalHistory(BaseModel):
    conditions: list[str] = []; medications: list[str] = []; allergies: list[str] = []

class MetabolicProfile(BaseModel):
    resting_metabolic_rate: int | None = None; glucose_response: str | None = None

class DietPreferences(BaseModel):
    preference: str; restrictions: list[str] = []; meal_timing: str | None = None

class SleepContext(BaseModel):
    chronotype: str | None = None; target_bedtime: str | None = None
    target_wake: str | None = None; environment_notes: str | None = None

class HealthProfile(BaseModel):
    biometrics: Biometrics; training: TrainingContext; medical: MedicalHistory
    metabolic: MetabolicProfile; diet: DietPreferences; supplements: list[Supplement]
    sleep_context: SleepContext; lab_values: dict[str, LabValue] = {}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Next.js project scaffold with Supabase DDL and shared infrastructure</name>
  <files>
    onboarding/package.json, onboarding/tsconfig.json, onboarding/next.config.ts,
    onboarding/tailwind.config.ts, onboarding/src/app/layout.tsx, onboarding/src/app/page.tsx,
    onboarding/src/app/onboarding/layout.tsx, onboarding/src/lib/supabase.ts,
    onboarding/src/lib/types.ts, onboarding/src/lib/completeness.ts,
    onboarding/src/components/onboarding/step-progress.tsx,
    onboarding/src/components/onboarding/step-navigation.tsx,
    onboarding/.env.local.example, supabase/onboarding-ddl.sql
  </files>
  <action>
    1. Create the Next.js project at `onboarding/` directory:
       ```bash
       npx create-next-app@15 --typescript --tailwind --app --src-dir onboarding
       ```
       Then install additional dependencies:
       ```bash
       cd onboarding && npm install react-hook-form @hookform/resolvers zod @supabase/supabase-js lucide-react
       ```
       Initialize shadcn/ui:
       ```bash
       npx shadcn@latest init
       npx shadcn@latest add button input select checkbox card progress textarea label radio-group
       ```

    2. Create `supabase/onboarding-ddl.sql` at repo root with:
       - `onboarding_profiles` table: UUID PK, step_1_complete through step_6_complete (boolean defaults false), onboarding_complete (boolean default false), step_1_data through step_6_data (JSONB defaults '{}'), created_at/updated_at TIMESTAMPTZ
       - `lab_results` table: UUID PK, profile_id FK, upload_date DATE, file_path TEXT, file_type TEXT, extraction_status TEXT default 'pending', extracted_values JSONB default '[]', confirmed_values JSONB default '[]', created_at TIMESTAMPTZ
       - `consent_records` table: UUID PK, profile_id FK, consent_type TEXT, consented BOOLEAN, consented_at TIMESTAMPTZ, consent_text TEXT
       - RLS policies allowing anon role full access on all three tables (single-user, no auth)
       - Supabase Storage bucket `lab-uploads` creation note (must be done via dashboard)
       - Trigger to auto-update `updated_at` on onboarding_profiles

    3. Create `onboarding/src/lib/supabase.ts`:
       - Supabase client singleton using `NEXT_PUBLIC_SUPABASE_URL` and `NEXT_PUBLIC_SUPABASE_ANON_KEY`
       - Export as named `supabase` constant

    4. Create `onboarding/src/lib/types.ts`:
       - `OnboardingProfile` interface matching the onboarding_profiles table structure
       - `LabResult` interface matching lab_results table
       - `ConsentRecord` interface matching consent_records table
       - `CompletenessResult` interface with percentage, completedSteps, totalSteps, incompleteSteps, suggestedNextStep

    5. Create `onboarding/src/lib/completeness.ts`:
       - `calculateCompleteness(profile: OnboardingProfile): CompletenessResult` function
       - Counts completed steps (step_1_complete through step_6_complete)
       - Returns percentage, completedSteps, incompleteSteps array, suggestedNextStep

    6. Create `onboarding/src/app/layout.tsx`:
       - Root layout with BioIntelligence branding: "BioIntelligence" title, subtitle "Precision AI / Evidence-Based Health Intelligence"
       - Medical disclaimer in small text at bottom: "AI research tool, not a medical service. Does not diagnose, treat, or replace clinical care."
       - Mobile-first responsive layout with max-w-2xl centered container

    7. Create `onboarding/src/app/page.tsx`:
       - Landing page that redirects to `/onboarding/step-1` (or shows a "Start Onboarding" button)

    8. Create `onboarding/src/app/onboarding/layout.tsx`:
       - Onboarding layout wrapper with the StepProgress component at top
       - Reads current step from URL path segment

    9. Create `onboarding/src/components/onboarding/step-progress.tsx`:
       - Visual progress indicator showing "Step X of 6" with step names
       - Step names: Biological Profile, Health & Medications, Metabolic & Nutrition, Training & Sleep, Baseline Biometrics, Data Upload & Consent
       - Highlight current step, show completed steps with checkmark, show skipped steps as accessible
       - Mobile-friendly horizontal layout

    10. Create `onboarding/src/components/onboarding/step-navigation.tsx`:
        - Back button (hidden on step 1), Next/Complete button, "Skip for now" ghost button
        - Props: currentStep, totalSteps (6), onSubmit callback, isSubmitting state, canSkip boolean
        - "Complete" text on final step instead of "Next"

    11. Create `onboarding/.env.local.example`:
        - NEXT_PUBLIC_SUPABASE_URL=
        - NEXT_PUBLIC_SUPABASE_ANON_KEY=
  </action>
  <verify>
    <automated>cd onboarding && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>Next.js project builds successfully, Supabase DDL file exists with all 3 tables + RLS policies, shared components and utilities are in place</done>
</task>

<task type="auto">
  <name>Task 2: Zod validation schemas for all 6 onboarding steps</name>
  <files>
    onboarding/src/lib/schemas/step-1.ts, onboarding/src/lib/schemas/step-2.ts,
    onboarding/src/lib/schemas/step-3.ts, onboarding/src/lib/schemas/step-4.ts,
    onboarding/src/lib/schemas/step-5.ts, onboarding/src/lib/schemas/step-6.ts
  </files>
  <action>
    Create one Zod schema file per step. Each file exports both the schema and the inferred TypeScript type. All field names, types, enums, and validation rules come from the CONTEXT.md field reference.

    **step-1.ts (Biological Profile):**
    Required: age (int 16-120), biological_sex (enum: male/female/prefer_not_to_say), height_cm (int 100-250), weight_kg (number 30-300), primary_sport (enum: running/cycling/triathlon/swimming/strength_training/crossfit_hiit/team_sports/hiking_trail/mixed_general_fitness/other)
    Optional: occupational_activity_level (enum 5 levels), hormonal_status (enum 8 options per CONTEXT.md -- ONBD-08), cycle_phase (enum 5 options -- ONBD-08), weekly_training_volume_hours (number 0-25), primary_goals (array of enum 9 options), perceived_stress_level (int 1-5)
    Export: step1Schema, Step1Data

    **step-2.ts (Health, Medications & Supplementation):**
    All optional: health_conditions (array of enum 12 options + "none"), injury_history_text (string), current_medications (string), smoking_status (enum 4 options), recovery_modalities (array of enum 7 options), supplements (object with 8 category arrays -- foundational/performance_recovery/hormonal_metabolic/longevity_cellular/cognitive_neurological/gut_immune/sleep_stress/ketogenic_metabolic -- each containing string array of supplement names), other_supplements_text (string), no_supplements (boolean)
    Export: step2Schema, Step2Data

    **step-3.ts (Metabolic & Nutrition Profile):**
    Required: dietary_pattern (enum 8 options), pre_training_nutrition (enum 7 options)
    Optional: primary_carb_sources (array of enum 9 options), metabolic_flexibility_signals (object with 5 fields -- energy_crash/hunger_irritability as enum Never/Occasionally/Often/Always, fasted_training as enum 4 options, carb_cravings as enum 4 options, energy_consistency as enum 4 options), eating_window (enum 8 options), estimated_daily_calories (enum 7 options), protein_intake_emphasis (enum 5 options), time_meal_to_training (enum 7 options), intra_training_fuelling (enum 5 options), post_workout_nutrition_window (enum 5 options), daily_water_intake (enum 4 options), pre_training_stimulants (array of enum 9 options), daily_caffeine_intake (enum 5 options), caffeine_cutoff_time (enum 6 options), alcohol_consumption (enum 4 options), food_sensitivities (array of enum 7 options)
    Export: step3Schema, Step3Data

    **step-4.ts (Training Context & Sleep):**
    Required: current_training_phase (enum 8 options: off_season/base_aerobic/build_race_specific/peak_competition/taper/recovery_deload/rehabilitation/no_structured_training), chronotype (enum 5 options: definite_morning/moderate_morning/intermediate/moderate_evening/definite_evening)
    Optional: next_race_event (string max 200 chars), typical_training_time (enum 7 options), sleep_schedule_consistency (enum 5 options), average_sleep_duration (enum 6 options), screen_blue_light (enum 5 options), subjective_recovery_waking (int 1-5), perceived_cognitive_fatigue (enum 4 options), preferred_insight_delivery_time (enum 4 options: morning/post_workout/evening/flexible)
    Export: step4Schema, Step4Data

    **step-5.ts (Baseline Biometric Metrics):**
    All optional: hrv_rmssd (number 0-300), resting_hr (number 30-120), vo2_max (number 15-90), spo2_avg (number 80-100), respiration_rate_sleep (number 5-30), body_battery_morning (int 0-100), average_daily_steps (int 0-50000), sleep_score_avg (int 0-100), garmin_training_status (enum 9 options)
    Export: step5Schema, Step5Data

    **step-6.ts (Data Upload & Informed Consent):**
    Optional: additional_context (string max 2000 chars)
    Required consent (validated at form level, not in this schema -- consent is stored separately):
    Note in comments that consent checkboxes are handled by the page component directly, not this schema. This schema only covers the free-text additional context field.
    Export: step6Schema, Step6Data

    IMPORTANT: All enum values should use snake_case to match Python-side expectations. This is the data contract between frontend and Python pipeline.
  </action>
  <verify>
    <automated>cd onboarding && npx tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <done>All 6 Zod schemas export correctly typed schemas and inferred types. TypeScript compilation passes with no errors. All fields from CONTEXT.md are represented with correct types, ranges, and enum values.</done>
</task>

</tasks>

<verification>
- `cd onboarding && npm run build` completes without errors
- `cd onboarding && npx tsc --noEmit` passes with no type errors
- `supabase/onboarding-ddl.sql` contains CREATE TABLE for all 3 tables with RLS policies
- All 6 schema files export both schema and type
- Completeness calculation returns correct values
</verification>

<success_criteria>
- Next.js project at `onboarding/` builds and dev server starts
- Supabase DDL ready to execute (3 tables, RLS, updated_at trigger)
- Zod schemas cover ALL fields from the 6-step questionnaire in CONTEXT.md
- Shared UI components (step-progress, step-navigation) render without errors
- Supabase client configured with NEXT_PUBLIC_ env vars
- Profile completeness calculation functional
</success_criteria>

<output>
After completion, create `.planning/phases/08-user-onboarding/08-01-SUMMARY.md`
</output>
