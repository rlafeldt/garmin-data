---
phase: 08-user-onboarding
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/biointelligence/profile/models.py
  - src/biointelligence/profile/loader.py
  - src/biointelligence/profile/onboarding_mapper.py
  - src/biointelligence/config.py
  - tests/test_profile.py
  - tests/test_onboarding_mapper.py
autonomous: true
requirements: [ONBD-03, ONBD-06, ONBD-08]

must_haves:
  truths:
    - "load_health_profile() queries Supabase first and falls back to YAML when no onboarding data exists"
    - "HealthProfile model accepts hormonal context fields (hormonal_status, cycle_phase) for female athletes"
    - "TrainingContext validator accepts all onboarding training phases (off_season, base_aerobic, etc.)"
    - "Onboarding JSONB data from Supabase maps correctly to HealthProfile model"
    - "Existing YAML profiles continue to load without changes (backwards compatibility)"
    - "All existing tests pass unchanged"
  artifacts:
    - path: "src/biointelligence/profile/models.py"
      provides: "Extended HealthProfile with hormonal context and expanded training phases"
      contains: "hormonal_status"
    - path: "src/biointelligence/profile/loader.py"
      provides: "Dual-mode loader: Supabase-first with YAML fallback"
      exports: ["load_health_profile"]
    - path: "src/biointelligence/profile/onboarding_mapper.py"
      provides: "Maps Supabase onboarding JSONB to HealthProfile model"
      exports: ["map_onboarding_to_health_profile"]
    - path: "tests/test_onboarding_mapper.py"
      provides: "Tests for JSONB-to-HealthProfile mapping"
      min_lines: 60
  key_links:
    - from: "src/biointelligence/profile/loader.py"
      to: "src/biointelligence/storage/supabase.py"
      via: "get_supabase_client for Supabase query"
      pattern: "get_supabase_client"
    - from: "src/biointelligence/profile/onboarding_mapper.py"
      to: "src/biointelligence/profile/models.py"
      via: "HealthProfile model construction from JSONB data"
      pattern: "HealthProfile\\("
    - from: "src/biointelligence/analysis/engine.py"
      to: "src/biointelligence/profile/loader.py"
      via: "load_health_profile called at analysis start"
      pattern: "load_health_profile"
---

<objective>
Extend the Python HealthProfile model with new onboarding fields (hormonal context, expanded training phases, metabolic flexibility signals), create a Supabase-first loader with YAML fallback, and build the mapping layer from Supabase JSONB to HealthProfile.

Purpose: Enables the analysis engine to consume onboarding data from Supabase while maintaining backwards compatibility with the existing YAML health profile. This is the critical Python-side integration that makes the web onboarding useful.
Output: Extended models, dual-mode loader, onboarding mapper, and comprehensive tests.
</objective>

<execution_context>
@/Users/raphaellafeldt/.claude/get-shit-done/workflows/execute-plan.md
@/Users/raphaellafeldt/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-user-onboarding/08-CONTEXT.md
@.planning/phases/08-user-onboarding/08-RESEARCH.md

<interfaces>
<!-- Current interfaces that will be modified -->
From src/biointelligence/profile/models.py:
```python
class TrainingContext(BaseModel):
    phase: str
    weekly_volume_hours: float
    preferred_types: list[str]
    race_goals: list[RaceGoal] = []
    injury_history: list[Injury] = []

    @field_validator("phase")
    @classmethod
    def validate_phase(cls, v: str) -> str:
        allowed = {"base", "build", "peak", "recovery"}
        if v not in allowed:
            msg = f"phase must be one of {allowed}"
            raise ValueError(msg)
        return v

class HealthProfile(BaseModel):
    biometrics: Biometrics
    training: TrainingContext
    medical: MedicalHistory
    metabolic: MetabolicProfile
    diet: DietPreferences
    supplements: list[Supplement]
    sleep_context: SleepContext
    lab_values: dict[str, LabValue] = {}
```

From src/biointelligence/profile/loader.py:
```python
def load_health_profile(path: Path) -> HealthProfile:
    """Load and validate a health profile from a YAML file."""
```

From src/biointelligence/analysis/engine.py:
```python
# Step 1: Load health profile (called with path from settings)
profile = load_health_profile(Path(settings.health_profile_path))
```

From src/biointelligence/config.py:
```python
class Settings(BaseSettings):
    health_profile_path: str = "health_profile.yaml"
    supabase_url: str
    supabase_key: str
```

From src/biointelligence/storage/supabase.py:
```python
def get_supabase_client(settings: Settings) -> Client:
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend HealthProfile model with onboarding fields and expand training phase validator</name>
  <files>src/biointelligence/profile/models.py, tests/test_profile.py</files>
  <behavior>
    - Test: TrainingContext accepts all onboarding phases: "off_season", "base_aerobic", "build_race_specific", "peak_competition", "taper", "recovery_deload", "rehabilitation", "no_structured_training" (plus existing: "base", "build", "peak", "recovery")
    - Test: Biometrics accepts new optional fields: primary_sport (str|None), occupational_activity_level (str|None), hormonal_status (str|None), cycle_phase (str|None), weekly_training_volume_hours (float|None), primary_goals (list[str]), perceived_stress_level (int|None)
    - Test: MetabolicProfile accepts new optional fields: dietary_pattern (str|None), pre_training_nutrition (str|None), metabolic_flexibility_signals (dict|None), eating_window (str|None), caffeine_intake (str|None)
    - Test: SleepContext accepts new optional fields: sleep_schedule_consistency (str|None), average_sleep_duration (str|None), subjective_recovery_waking (int|None), preferred_insight_delivery_time (str|None)
    - Test: MedicalHistory accepts new optional fields: smoking_status (str|None), recovery_modalities (list[str]), supplement_categories (dict[str, list[str]])
    - Test: Existing YAML fixture profile still loads and validates without changes
    - Test: HealthProfile with all new optional fields set to None still validates
  </behavior>
  <action>
    1. Expand `TrainingContext.validate_phase()` allowed set to include all onboarding training phases: {"base", "build", "peak", "recovery", "off_season", "base_aerobic", "build_race_specific", "peak_competition", "taper", "recovery_deload", "rehabilitation", "no_structured_training"}. Also make `weekly_volume_hours` optional (float | None = None) and `preferred_types` default to empty list, since onboarding doesn't always collect these.

    2. Add optional fields to `Biometrics`: primary_sport (str | None = None), occupational_activity_level (str | None = None), hormonal_status (str | None = None), cycle_phase (str | None = None), primary_goals (list[str] = []), perceived_stress_level (int | None = None). These cover ONBD-08 (female athlete hormonal context).

    3. Add optional fields to `MetabolicProfile`: dietary_pattern (str | None = None), pre_training_nutrition (str | None = None), metabolic_flexibility_signals (dict[str, str] | None = None), eating_window (str | None = None), caffeine_intake (str | None = None), caffeine_cutoff (str | None = None), alcohol_consumption (str | None = None), protein_emphasis (str | None = None), food_sensitivities (list[str] = [])

    4. Add optional fields to `SleepContext`: sleep_schedule_consistency (str | None = None), average_sleep_duration (str | None = None), subjective_recovery_waking (int | None = None), perceived_cognitive_fatigue (str | None = None), screen_blue_light (str | None = None), preferred_insight_delivery_time (str | None = None)

    5. Add optional fields to `MedicalHistory`: smoking_status (str | None = None), recovery_modalities (list[str] = []), supplement_categories (dict[str, list[str]] = {}), other_supplements_text (str | None = None), no_supplements (bool = False)

    6. All new fields MUST be optional with defaults so existing YAML profiles and tests pass unchanged. Use `X | None = None` syntax (modern Python, per ruff UP045 from Phase 1 decisions).

    7. Update tests in test_profile.py: add test for expanded training phases, test for new Biometrics fields (including hormonal context), test that existing fixture still loads.
  </action>
  <verify>
    <automated>source .venv/bin/activate && python -m pytest tests/test_profile.py -x -v 2>&1 | tail -20</automated>
  </verify>
  <done>HealthProfile model extended with all onboarding fields. All new fields are optional with None defaults. TrainingContext accepts all 12 training phase values. Existing YAML profile and all prior tests pass unchanged. New tests verify extended fields and hormonal context (ONBD-08).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Supabase-first health profile loader with YAML fallback and onboarding mapper</name>
  <files>
    src/biointelligence/profile/loader.py,
    src/biointelligence/profile/onboarding_mapper.py,
    tests/test_onboarding_mapper.py,
    tests/test_profile.py
  </files>
  <behavior>
    - Test: map_onboarding_to_health_profile converts JSONB step data into valid HealthProfile
    - Test: Mapper handles partial data (only step 1 complete, others empty {})
    - Test: Mapper maps hormonal context from step 1 to Biometrics fields
    - Test: Mapper maps supplement categories from step 2 to MedicalHistory fields
    - Test: Mapper maps metabolic flexibility signals from step 3 to MetabolicProfile
    - Test: Mapper maps training phase and chronotype from step 4 to TrainingContext/SleepContext
    - Test: Mapper maps baseline biometrics from step 5 (stored as reference, not used in HealthProfile directly)
    - Test: load_health_profile queries Supabase, returns mapped profile when data exists
    - Test: load_health_profile falls back to YAML when Supabase query returns no rows
    - Test: load_health_profile falls back to YAML when Supabase query throws an exception
  </behavior>
  <action>
    1. Create `src/biointelligence/profile/onboarding_mapper.py`:
       - `map_onboarding_to_health_profile(row: dict) -> HealthProfile` function
       - Takes a single row from the onboarding_profiles table (dict with step_1_data through step_6_data as dicts)
       - Maps step_1_data fields to Biometrics: age, biological_sex -> sex, height_cm, weight_kg, primary_sport, occupational_activity_level, hormonal_status, cycle_phase, primary_goals, perceived_stress_level
       - Maps step_2_data fields to MedicalHistory: health_conditions -> conditions, current_medications -> medications (split by comma if string), smoking_status, recovery_modalities, supplement_categories, other_supplements_text, no_supplements. Also extract supplements list for HealthProfile.supplements (convert category arrays to Supplement objects with name only, dose/form/timing as "per user" defaults).
       - Maps step_3_data fields to DietPreferences: dietary_pattern -> preference, food_sensitivities -> restrictions. Also to MetabolicProfile: metabolic_flexibility_signals, eating_window, caffeine_intake, caffeine_cutoff, alcohol_consumption, protein_emphasis, pre_training_nutrition.
       - Maps step_4_data fields to TrainingContext: current_training_phase -> phase (map onboarding values to allowed set, e.g., "base_aerobic" stays as "base_aerobic"), next_race_event -> race_goals. Maps to SleepContext: chronotype, sleep_schedule_consistency, average_sleep_duration, subjective_recovery_waking, perceived_cognitive_fatigue, screen_blue_light, preferred_insight_delivery_time.
       - step_5_data: Baseline biometrics are informational (used by AI for context, not mapped to HealthProfile model directly). Store as-is in a separate section if needed.
       - step_6_data: Additional context text -- could be stored in a new HealthProfile field or passed separately.
       - Handle missing/empty step data gracefully: default empty dicts, use .get() with fallbacks.
       - Log the mapping with structlog (matching existing patterns).

    2. Modify `src/biointelligence/profile/loader.py`:
       - Change `load_health_profile(path: Path, settings: Settings | None = None) -> HealthProfile` signature to accept optional settings parameter
       - If settings is None, import and call `get_settings()` (lazy, to avoid circular imports)
       - Try Supabase first: call `get_supabase_client(settings)`, query `onboarding_profiles` table with `.select("*").limit(1).execute()`
       - If response.data is non-empty, call `map_onboarding_to_health_profile(response.data[0])` and return
       - On empty result or any exception, log warning and fall through to YAML loader
       - YAML fallback: existing logic (open file, yaml.safe_load, model_validate) -- unchanged
       - Import `get_supabase_client` from storage.supabase and `map_onboarding_to_health_profile` from onboarding_mapper

    3. Update `src/biointelligence/analysis/engine.py` call site: The existing `load_health_profile(Path(settings.health_profile_path))` call needs the settings parameter added: `load_health_profile(Path(settings.health_profile_path), settings=settings)`. This enables the Supabase query path.

    4. Create `tests/test_onboarding_mapper.py`:
       - Test full mapping with all 6 steps populated
       - Test partial mapping (only step 1 data)
       - Test empty step data defaults gracefully
       - Test hormonal context mapping (ONBD-08)
       - Test supplement categories to supplements list

    5. Add tests to `tests/test_profile.py`:
       - Test: load_health_profile with mocked Supabase returning data -> returns mapped HealthProfile
       - Test: load_health_profile with mocked Supabase returning empty -> falls back to YAML
       - Test: load_health_profile with mocked Supabase throwing exception -> falls back to YAML

    Use pytest-mock (mocker fixture) for mocking Supabase calls, consistent with existing test patterns.
  </action>
  <verify>
    <automated>source .venv/bin/activate && python -m pytest tests/test_profile.py tests/test_onboarding_mapper.py -x -v 2>&1 | tail -20</automated>
  </verify>
  <done>load_health_profile() queries Supabase first and falls back to YAML (ONBD-06). Onboarding JSONB maps to HealthProfile correctly. Hormonal context fields flow through (ONBD-08). All existing tests pass. New mapper tests cover full, partial, and empty data scenarios.</done>
</task>

</tasks>

<verification>
- `source .venv/bin/activate && python -m pytest tests/ -x -q` -- full test suite passes
- TrainingContext accepts both old phases ("base", "build", "peak", "recovery") and new onboarding phases
- HealthProfile model has hormonal_status and cycle_phase fields (ONBD-08)
- load_health_profile tries Supabase, falls back to YAML (ONBD-06)
- Existing YAML fixture loads without changes (backwards compatibility)
</verification>

<success_criteria>
- All existing 41+ tests pass unchanged
- New model fields are all optional with None defaults
- Onboarding mapper handles all 6 steps with graceful fallbacks for missing data
- load_health_profile() dual-mode behavior verified by tests
- ONBD-03 (Supabase data source), ONBD-06 (YAML fallback), ONBD-08 (hormonal context) requirements addressed
</success_criteria>

<output>
After completion, create `.planning/phases/08-user-onboarding/08-02-SUMMARY.md`
</output>
