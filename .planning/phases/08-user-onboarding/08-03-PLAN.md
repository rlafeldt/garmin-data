---
phase: 08-user-onboarding
plan: 03
type: execute
wave: 2
depends_on: [08-01]
files_modified:
  - onboarding/src/app/onboarding/step-1/page.tsx
  - onboarding/src/app/onboarding/step-2/page.tsx
  - onboarding/src/app/onboarding/step-3/page.tsx
  - onboarding/src/app/onboarding/step-4/page.tsx
  - onboarding/src/components/onboarding/multi-select.tsx
  - onboarding/src/components/onboarding/supplement-picker.tsx
  - onboarding/src/components/onboarding/field-group.tsx
  - onboarding/src/hooks/use-step-form.ts
autonomous: true
requirements: [ONBD-01, ONBD-02, ONBD-05, ONBD-08]

must_haves:
  truths:
    - "Step 1 form collects and persists all biological profile fields to Supabase"
    - "Step 2 form presents supplement picker with 8 categorized groups and persists to Supabase"
    - "Step 3 form collects metabolic flexibility signals and nutrition data"
    - "Step 4 form collects training phase, chronotype, and sleep context"
    - "Hormonal context fields (status, cycle phase) shown conditionally for female athletes"
    - "Required fields show validation errors, optional fields can be skipped"
    - "Each step loads previously saved data when revisited (resume capability)"
    - "User can navigate back to previous steps"
  artifacts:
    - path: "onboarding/src/app/onboarding/step-1/page.tsx"
      provides: "Biological Profile form with hormonal context conditional fields"
      min_lines: 80
    - path: "onboarding/src/app/onboarding/step-2/page.tsx"
      provides: "Health & Supplements form with categorized supplement picker"
      min_lines: 80
    - path: "onboarding/src/components/onboarding/supplement-picker.tsx"
      provides: "Categorized multi-select for 60+ supplements across 8 groups"
      min_lines: 60
    - path: "onboarding/src/hooks/use-step-form.ts"
      provides: "Custom hook encapsulating RHF + Supabase load/save pattern"
      exports: ["useStepForm"]
  key_links:
    - from: "onboarding/src/app/onboarding/step-1/page.tsx"
      to: "onboarding/src/lib/schemas/step-1.ts"
      via: "zodResolver(step1Schema)"
      pattern: "zodResolver"
    - from: "onboarding/src/hooks/use-step-form.ts"
      to: "onboarding/src/lib/supabase.ts"
      via: "Supabase read on mount, write on submit"
      pattern: "supabase.*from.*onboarding_profiles"
    - from: "onboarding/src/app/onboarding/step-1/page.tsx"
      to: "onboarding/src/components/onboarding/step-navigation.tsx"
      via: "StepNavigation component for back/next/skip"
      pattern: "StepNavigation"
---

<objective>
Build wizard steps 1-4 (Biological Profile, Health & Medications, Metabolic & Nutrition, Training & Sleep) with full form state management, Supabase persistence, and resume capability.

Purpose: Implements the core onboarding data collection UI for the first 4 steps. These steps contain the essential fields (ONBD-02) and the female athlete hormonal context (ONBD-08). Each step saves to Supabase immediately, enabling resume (ONBD-05).
Output: Four fully functional wizard step pages with validation, persistence, and navigation.
</objective>

<execution_context>
@/Users/raphaellafeldt/.claude/get-shit-done/workflows/execute-plan.md
@/Users/raphaellafeldt/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-user-onboarding/08-CONTEXT.md
@.planning/phases/08-user-onboarding/08-RESEARCH.md
@.planning/phases/08-user-onboarding/08-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 outputs (schemas, components, supabase client) -->
From onboarding/src/lib/schemas/step-1.ts:
```typescript
export const step1Schema = z.object({
  age: z.number().int().min(16).max(120),
  biological_sex: z.enum(["male", "female", "prefer_not_to_say"]),
  height_cm: z.number().int().min(100).max(250),
  weight_kg: z.number().min(30).max(300),
  primary_sport: z.enum([...]),
  // Optional fields...
  hormonal_status: z.enum([...]).optional(),
  cycle_phase: z.enum([...]).optional(),
});
export type Step1Data = z.infer<typeof step1Schema>;
```

From onboarding/src/lib/supabase.ts:
```typescript
export const supabase = createClient(url, key);
```

From onboarding/src/components/onboarding/step-navigation.tsx:
```typescript
export function StepNavigation({
  currentStep, totalSteps, onSubmit, isSubmitting, canSkip
}: StepNavigationProps)
```

From onboarding/src/components/onboarding/step-progress.tsx:
```typescript
export function StepProgress({ currentStep, completedSteps }: StepProgressProps)
```

From onboarding/src/lib/types.ts:
```typescript
export interface OnboardingProfile {
  id: string;
  step_1_complete: boolean; step_1_data: Record<string, unknown>;
  // ... through step_6
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: useStepForm hook, multi-select component, field-group wrapper, and Step 1-2 pages</name>
  <files>
    onboarding/src/hooks/use-step-form.ts,
    onboarding/src/components/onboarding/multi-select.tsx,
    onboarding/src/components/onboarding/supplement-picker.tsx,
    onboarding/src/components/onboarding/field-group.tsx,
    onboarding/src/app/onboarding/step-1/page.tsx,
    onboarding/src/app/onboarding/step-2/page.tsx
  </files>
  <action>
    1. Create `onboarding/src/hooks/use-step-form.ts`:
       - Custom hook `useStepForm<T>(stepNumber: number, schema: ZodSchema<T>)` that encapsulates the RHF + Supabase pattern
       - On mount: query `onboarding_profiles` table for existing step data (`step_{n}_data`), use RHF `reset()` to populate form
       - On submit: upsert to `onboarding_profiles` with `step_{n}_data: values, step_{n}_complete: true`
       - Returns: `{ form, onSubmit, isLoading, isSubmitting, profileId }` where form is the RHF useForm return
       - Handle the single-row pattern: always query with `.limit(1)`, create row on first save if none exists
       - Use `defaultValues` async function from RHF v7+ for initial load, show loading skeleton while fetching
       - After successful submit, navigate to next step via `router.push`

    2. Create `onboarding/src/components/onboarding/field-group.tsx`:
       - Wrapper component for form field groups with label, description, and optional "required" indicator
       - Props: label (string), description? (string), required? (boolean), children
       - Renders shadcn/ui Label with consistent spacing

    3. Create `onboarding/src/components/onboarding/multi-select.tsx`:
       - Reusable multi-select component using shadcn/ui Checkbox components
       - Props: options (array of {value, label}), value (string[]), onChange ((values: string[]) => void)
       - Renders as a grid of checkboxes (2 columns on mobile, 3 on desktop)
       - Integrates with RHF via Controller

    4. Create `onboarding/src/components/onboarding/supplement-picker.tsx`:
       - Categorized supplement selector with 8 expandable groups from CONTEXT.md
       - Categories: Foundational, Performance & Recovery, Hormonal & Metabolic, Longevity & Cellular, Cognitive & Neurological, Gut & Immune, Sleep & Stress, Ketogenic / Metabolic Support
       - Each category header is clickable to expand/collapse the list
       - Items within each category are checkboxes
       - Stores as flat object: `{ foundational: ["vitamin_d3_k2", "magnesium"], performance_recovery: ["creatine"], ... }`
       - Include a "No supplements" checkbox that clears all selections
       - All supplement names use snake_case identifiers
       - PITFALL: Must be responsive and not overwhelm the user. Categories collapsed by default, expand on tap.

    5. Create `onboarding/src/app/onboarding/step-1/page.tsx`:
       - "use client" component
       - Uses useStepForm(1, step1Schema)
       - REQUIRED section: Age (number input), Biological Sex (radio group: Male/Female/Prefer not to say), Height cm (number input), Weight kg (number input), Primary Sport (select dropdown)
       - OPTIONAL section: Occupational Activity Level (select), Weekly Training Volume (number 0-25 slider or input), Primary Goals (multi-select checkboxes), Perceived Stress Level (1-5 radio or slider)
       - CONDITIONAL (ONBD-08): When biological_sex === "female", show Hormonal Context section with Hormonal/Menstrual Status (select) and Current Cycle Phase (select). Hide when male or prefer_not_to_say.
       - Clear visual separation between required and optional sections
       - Uses StepNavigation with canSkip=false (Step 1 has required fields)

    6. Create `onboarding/src/app/onboarding/step-2/page.tsx`:
       - "use client" component
       - Uses useStepForm(2, step2Schema)
       - Health Conditions (multi-select with "None" option that clears others)
       - Injury History/Clinical Context (textarea)
       - Current Medications (textarea, optional)
       - Smoking/Vaping Status (radio group)
       - Recovery Modalities (multi-select)
       - Supplementation section using the SupplementPicker component
       - Other Supplements/Dosages/Brands (textarea)
       - Uses StepNavigation with canSkip=true (all fields optional)
  </action>
  <verify>
    <automated>cd onboarding && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>Steps 1 and 2 render with all fields from CONTEXT.md. useStepForm hook loads from and saves to Supabase. Hormonal context fields appear conditionally for female athletes (ONBD-08). Supplement picker shows all 8 categories. Build passes.</done>
</task>

<task type="auto">
  <name>Task 2: Step 3 (Metabolic & Nutrition) and Step 4 (Training & Sleep) pages</name>
  <files>
    onboarding/src/app/onboarding/step-3/page.tsx,
    onboarding/src/app/onboarding/step-4/page.tsx
  </files>
  <action>
    1. Create `onboarding/src/app/onboarding/step-3/page.tsx`:
       - "use client" component
       - Uses useStepForm(3, step3Schema)
       - REQUIRED section: Dietary Pattern (select from 8 options), Pre-Training Nutrition (select from 7 options)
       - OPTIONAL Metabolic Flexibility section with 5 signal fields:
         - Energy crash/brain fog after carb meal (Never/Occasionally/Often/Always radio)
         - Hunger/irritability when skipping meal (same scale)
         - Ability to train fasted (Never tried/Cannot/With difficulty/Easily)
         - Carb cravings afternoon/evening (same 4-point scale)
         - Energy consistency throughout day (Always consistent/Mostly/Variable/Significant crashes)
       - OPTIONAL Nutrition Details section:
         - Primary Carbohydrate Sources (multi-select 9 options)
         - Eating Window/Fasting Protocol (select)
         - Estimated Daily Calories (select)
         - Protein Intake Emphasis (select)
         - Time Between Last Meal and Training (select)
         - Intra-Training Fuelling (select)
         - Post-Workout Nutrition Window (select)
         - Daily Water Intake (select)
       - OPTIONAL Stimulants section:
         - Pre-Training Stimulants (multi-select 9 options)
         - Daily Caffeine Intake (select)
         - Caffeine Cut-Off Time (select)
       - OPTIONAL: Alcohol Consumption (select), Food Sensitivities (multi-select)
       - Uses StepNavigation with canSkip=true for optional fields (but required fields must be filled to proceed without skip)
       - NOTE: This is the longest form. Group fields clearly with visual separators (card sections or fieldset borders). Use FieldGroup component for each section.

    2. Create `onboarding/src/app/onboarding/step-4/page.tsx`:
       - "use client" component
       - Uses useStepForm(4, step4Schema)
       - REQUIRED section: Current Training Phase (select from 8 options matching expanded TrainingContext validator), Chronotype (select from 5 options: Definite morning/Moderate morning/Intermediate/Moderate evening/Definite evening)
       - OPTIONAL Training Context section:
         - Next Race/Key Event (text input, max 200 chars)
         - Typical Training Time of Day (select 7 options)
       - OPTIONAL Sleep Context section:
         - Sleep Schedule Consistency (select 5 options)
         - Average Sleep Duration (select 6 options)
         - Screen/Blue Light Exposure Before Bed (select 5 options)
         - Subjective Recovery on Waking (1-5 scale, radio group with labels: Exhausted/Below average/Moderate/Good/Fully restored)
         - Perceived Cognitive Fatigue (select 4 options)
       - OPTIONAL Delivery Preference:
         - Preferred Insight Delivery Time (select: Morning/Post-workout/Evening/Flexible)
         - Small note: "This preference will be used for future delivery scheduling"
       - Uses StepNavigation with canSkip=true for optional fields
  </action>
  <verify>
    <automated>cd onboarding && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>Steps 3 and 4 render with all fields from CONTEXT.md. Metabolic flexibility signals presented as clear grouped questions. Training phase options match the expanded Python TrainingContext validator. All data saved to Supabase via useStepForm hook. Build passes.</done>
</task>

</tasks>

<verification>
- `cd onboarding && npm run build` completes without errors
- All 4 step pages import their respective Zod schemas and use useStepForm
- Step 1 shows hormonal context conditionally for female athletes
- Step 2 supplement picker has all 8 categories with correct items
- Steps 3-4 have all fields from CONTEXT.md
- Back/Next/Skip navigation works across all steps
</verification>

<success_criteria>
- Steps 1-4 fully implemented with all fields from CONTEXT.md
- Essential fields (age, sex, height, weight, sport, dietary pattern, training phase, chronotype) are required with validation
- Non-essential fields marked "Skip for now" or presented as optional
- Data persisted to Supabase per step via upsert
- Previously saved data loads when revisiting a step (resume capability)
- Hormonal context conditional display for female athletes (ONBD-08)
- Build passes with no TypeScript errors
</success_criteria>

<output>
After completion, create `.planning/phases/08-user-onboarding/08-03-SUMMARY.md`
</output>
