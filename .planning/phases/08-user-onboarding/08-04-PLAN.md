---
phase: 08-user-onboarding
plan: 04
type: execute
wave: 2
depends_on: [08-01]
files_modified:
  - onboarding/src/app/onboarding/step-5/page.tsx
  - onboarding/src/app/onboarding/step-6/page.tsx
  - onboarding/src/app/onboarding/complete/page.tsx
  - onboarding/src/app/profile/page.tsx
  - onboarding/src/components/onboarding/lab-upload.tsx
  - onboarding/src/components/onboarding/consent-checkboxes.tsx
  - onboarding/src/app/api/extract-lab/route.ts
autonomous: true
requirements: [ONBD-01, ONBD-04, ONBD-05, ONBD-07]

must_haves:
  truths:
    - "Step 5 collects optional baseline biometric metrics with clear explanations"
    - "Step 6 presents file upload for lab results and requires all 3 consent checkboxes before completion"
    - "Lab upload stores files in Supabase Storage and triggers extraction via API route"
    - "Onboarding cannot complete without all 3 consent checkboxes checked (ONBD-04)"
    - "Completion page shows profile summary with completeness percentage"
    - "Profile edit page allows updating any step after initial onboarding (ONBD-05)"
    - "Extracted lab values shown in editable fields for user review before saving"
  artifacts:
    - path: "onboarding/src/app/onboarding/step-6/page.tsx"
      provides: "Data upload and consent form with 3 required checkboxes"
      min_lines: 80
    - path: "onboarding/src/components/onboarding/lab-upload.tsx"
      provides: "File upload to Supabase Storage with extraction status display"
      min_lines: 60
    - path: "onboarding/src/components/onboarding/consent-checkboxes.tsx"
      provides: "Three informed consent checkboxes with exact text from CONTEXT.md"
      min_lines: 40
    - path: "onboarding/src/app/api/extract-lab/route.ts"
      provides: "Server-side API route calling Anthropic for lab value extraction"
      exports: ["POST"]
    - path: "onboarding/src/app/profile/page.tsx"
      provides: "Profile edit page with step-by-step navigation"
      min_lines: 40
  key_links:
    - from: "onboarding/src/components/onboarding/lab-upload.tsx"
      to: "onboarding/src/lib/supabase.ts"
      via: "Supabase Storage upload"
      pattern: "supabase.storage.from.*lab-uploads"
    - from: "onboarding/src/app/api/extract-lab/route.ts"
      to: "Anthropic API"
      via: "Claude Vision/PDF extraction call"
      pattern: "anthropic.*messages.create"
    - from: "onboarding/src/app/onboarding/step-6/page.tsx"
      to: "onboarding/src/components/onboarding/consent-checkboxes.tsx"
      via: "All 3 checkboxes required for form submission"
      pattern: "consent.*checked"
---

<objective>
Build wizard steps 5-6 (Baseline Biometrics, Data Upload & Consent), the completion page, the profile edit page, and the lab extraction API route.

Purpose: Completes the 6-step wizard with consent gates (ONBD-04), lab upload with extraction (ONBD-07), profile summary, and post-onboarding profile editing (ONBD-05).
Output: Steps 5-6, completion page, profile edit page, lab extraction API route, consent and upload components.
</objective>

<execution_context>
@/Users/raphaellafeldt/.claude/get-shit-done/workflows/execute-plan.md
@/Users/raphaellafeldt/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-user-onboarding/08-CONTEXT.md
@.planning/phases/08-user-onboarding/08-RESEARCH.md
@.planning/phases/08-user-onboarding/08-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 outputs -->
From onboarding/src/lib/schemas/step-5.ts:
```typescript
export const step5Schema = z.object({
  hrv_rmssd: z.number().min(0).max(300).optional(),
  resting_hr: z.number().min(30).max(120).optional(),
  vo2_max: z.number().min(15).max(90).optional(),
  // ... all optional baseline biometrics
});
export type Step5Data = z.infer<typeof step5Schema>;
```

From onboarding/src/lib/schemas/step-6.ts:
```typescript
export const step6Schema = z.object({
  additional_context: z.string().max(2000).optional(),
});
export type Step6Data = z.infer<typeof step6Schema>;
```

From onboarding/src/hooks/use-step-form.ts:
```typescript
export function useStepForm<T>(stepNumber: number, schema: ZodSchema<T>): {
  form: UseFormReturn<T>; onSubmit: (values: T) => Promise<void>;
  isLoading: boolean; isSubmitting: boolean; profileId: string | null;
}
```

From onboarding/src/lib/supabase.ts:
```typescript
export const supabase = createClient(url, key);
```

From onboarding/src/lib/completeness.ts:
```typescript
export function calculateCompleteness(profile: OnboardingProfile): CompletenessResult;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Step 5-6 pages, consent checkboxes, and lab upload component</name>
  <files>
    onboarding/src/app/onboarding/step-5/page.tsx,
    onboarding/src/app/onboarding/step-6/page.tsx,
    onboarding/src/components/onboarding/consent-checkboxes.tsx,
    onboarding/src/components/onboarding/lab-upload.tsx
  </files>
  <action>
    1. Create `onboarding/src/components/onboarding/consent-checkboxes.tsx`:
       - Three checkboxes with EXACT text from CONTEXT.md:
         a) "I understand that BioIntelligence is an AI research tool that synthesises peer-reviewed scientific literature. It does not provide medical diagnoses, prescribe treatments, or replace the advice of a qualified healthcare practitioner."
         b) "I consent to my health and biometric data being processed to generate personalised insights. My data will not be sold or shared with third parties."
         c) "I understand that all insights reflect the application of scientific literature to my individual data, and that I should seek clinical evaluation for any health concerns raised by these insights."
       - Props: values ({ai_disclaimer: boolean, data_processing: boolean, clinical_evaluation: boolean}), onChange callback
       - Each checkbox is a shadcn/ui Checkbox with the full text as label
       - Visual indicator when all 3 are checked (green border or checkmark)
       - Export `allConsentsGiven(values)` helper that returns true only when all 3 are true

    2. Create `onboarding/src/components/onboarding/lab-upload.tsx`:
       - File upload component accepting PDF and image files (image/jpeg, image/png, application/pdf)
       - Uses native input[type=file] with accept attribute
       - On file select: upload to Supabase Storage bucket "lab-uploads" with path `{profileId}/{timestamp}.{ext}`
       - After upload: call `/api/extract-lab` POST endpoint with {file_path, file_type, profile_id}
       - Show extraction status: uploading -> extracting -> review
       - Display extracted values in editable input fields (marker name, value, unit, reference range)
       - Confidence indicator per value: green (>=0.8), yellow (0.5-0.79), red (<0.5) using colored dot
       - "Confirm" button saves confirmed_values to lab_results table
       - Support multiple uploads -- show list of uploaded files with extraction status
       - Date picker for each upload (lab test date)
       - File size limit: 10MB check before upload

    3. Create `onboarding/src/app/onboarding/step-5/page.tsx`:
       - "use client" component
       - Uses useStepForm(5, step5Schema)
       - ALL fields optional with explanatory descriptions:
         - HRV (ms, RMSSD) -- "30-day average from Garmin Connect"
         - RHR (bpm) -- "Resting Heart Rate average"
         - VO2 Max (ml/kg/min) -- "Garmin estimate"
         - SpO2 (%) -- "Blood Oxygen Saturation"
         - Respiration Rate during sleep (brpm)
         - Body Battery morning score (0-100)
         - Average daily steps
         - Sleep score average (0-100)
         - Garmin Training Status (select: Unproductive/Maintaining/Productive/Peaking/Overreaching/Recovery/Detraining/Not sure)
       - Informational note at top: "These are optional. If left blank, the AI will establish your personal baseline from the first 7 days of Garmin data."
       - Uses FieldGroup for each metric with unit labels
       - canSkip=true (all fields optional)

    4. Create `onboarding/src/app/onboarding/step-6/page.tsx`:
       - "use client" component
       - Two sections: Upload and Consent
       - UPLOAD section:
         - LabUpload component for lab results/bloodwork (PDF or image)
         - Additional Context textarea (free text, max 2000 chars): "Recent life events, illness, travel, training blocks, race schedule, stressors that may affect your data"
         - Uses useStepForm(6, step6Schema) for the text field
       - CONSENT section (ONBD-04):
         - ConsentCheckboxes component with all 3 required checkboxes
         - Consent values NOT stored in step_6_data -- stored separately in consent_records table
         - On "Complete" button: validate all 3 consents checked, save consent records to consent_records table, set onboarding_complete=true on onboarding_profiles, navigate to /onboarding/complete
         - "Complete" button disabled until all 3 consents are checked
         - Show clear error message if user tries to complete without all consents
       - canSkip=false on this step (consent is required)
       - Note: Lab upload is optional, but consent is mandatory
  </action>
  <verify>
    <automated>cd onboarding && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>Steps 5-6 render with all fields. Consent checkboxes have exact text from CONTEXT.md. Lab upload component handles file upload to Supabase Storage. Completion blocked without all 3 consents (ONBD-04). Build passes.</done>
</task>

<task type="auto">
  <name>Task 2: Lab extraction API route, completion page, and profile edit page</name>
  <files>
    onboarding/src/app/api/extract-lab/route.ts,
    onboarding/src/app/onboarding/complete/page.tsx,
    onboarding/src/app/profile/page.tsx
  </files>
  <action>
    1. Create `onboarding/src/app/api/extract-lab/route.ts`:
       - Next.js API route (App Router): export async function POST(request: Request)
       - Accepts JSON body: { file_path: string, file_type: string, profile_id: string }
       - Downloads file from Supabase Storage using service-side Supabase client (use SUPABASE_SERVICE_ROLE_KEY env var, NOT the anon key -- this is server-side only)
       - Calls Anthropic API via TypeScript SDK (@anthropic-ai/sdk) for extraction
       - Use claude-haiku-4-5-20251001 model for cost-effective extraction
       - Prompt: "Extract lab values from this document. Target markers: [list of 20 markers from RESEARCH.md]. For each found marker, return: marker_name, value (numeric), unit, reference_range. Assign a confidence score (0.0-1.0). Return JSON array. Only extract markers you can clearly identify."
       - For PDFs: use document content block type with base64 encoding
       - For images: use image content block type with base64 encoding
       - Parse response as JSON array of {marker_name, value, unit, reference_range, confidence}
       - Update lab_results table: set extraction_status='extracted', extracted_values=result
       - Return JSON response with extracted values
       - Error handling: return 500 with error message on extraction failure, update lab_results status to 'failed'
       - Add `@anthropic-ai/sdk` to package.json dependencies:
         ```bash
         cd onboarding && npm install @anthropic-ai/sdk
         ```
       - Env vars needed (server-side, NOT NEXT_PUBLIC_): ANTHROPIC_API_KEY, SUPABASE_SERVICE_ROLE_KEY, SUPABASE_URL (reuse without NEXT_PUBLIC_ prefix)
       - Update .env.local.example with these additional vars

    2. Create `onboarding/src/app/onboarding/complete/page.tsx`:
       - Congratulatory completion page with BioIntelligence branding
       - Load profile from Supabase and show completeness summary using calculateCompleteness()
       - Show completed steps with checkmarks, incomplete steps with "Complete later" links
       - Overall completion percentage bar
       - Message: "Your profile is ready. BioIntelligence will use this data to personalise your Daily Protocol."
       - If some steps were skipped: "You skipped some optional sections. You can complete them anytime from your profile page, or we will remind you via WhatsApp when more data would improve your insights."
       - Link to "/profile" for editing
       - Medical disclaimer reminder

    3. Create `onboarding/src/app/profile/page.tsx`:
       - Profile edit/view page (ONBD-05)
       - Load onboarding profile from Supabase
       - Show each step as a card with:
         - Step name and completion status (checkmark or "Incomplete")
         - Summary of key data entered (e.g., "Age: 32, Height: 175cm, Weight: 72kg")
         - "Edit" button linking to /onboarding/step-{n} (reuses wizard pages for editing)
       - Show overall completeness bar at top
       - Show lab results section with uploaded files and their extraction status
       - "Upload New Lab Results" button linking to /onboarding/step-6
       - Show consent status
       - Medical disclaimer at bottom
       - This page can be deep-linked from WhatsApp nudges
  </action>
  <verify>
    <automated>cd onboarding && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>Lab extraction API route calls Anthropic and stores results. Completion page shows profile summary. Profile edit page allows updating any step (ONBD-05). All 3 pages render and build passes.</done>
</task>

</tasks>

<verification>
- `cd onboarding && npm run build` completes without errors
- Step 6 requires all 3 consent checkboxes (try submitting without -- should show error)
- Lab upload component handles PDF and image files
- API route has proper error handling for extraction failures
- Completion page shows accurate completeness percentage
- Profile page links back to individual steps for editing
</verification>

<success_criteria>
- Complete 6-step wizard with all pages functional
- Consent gate blocks completion without all 3 checkboxes (ONBD-04)
- Lab upload stores files in Supabase Storage (ONBD-07)
- Lab extraction via Anthropic API returns structured values with confidence scores (ONBD-07)
- User can review and edit extracted lab values before confirming (ONBD-07)
- Profile edit page allows updating any step after initial onboarding (ONBD-05)
- Completion page shows summary and completeness
- Build passes with no TypeScript errors
</success_criteria>

<output>
After completion, create `.planning/phases/08-user-onboarding/08-04-SUMMARY.md`
</output>
