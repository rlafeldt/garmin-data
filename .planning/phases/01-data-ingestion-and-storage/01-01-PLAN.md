---
phase: 01-data-ingestion-and-storage
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - src/biointelligence/__init__.py
  - src/biointelligence/config.py
  - src/biointelligence/logging.py
  - src/biointelligence/garmin/__init__.py
  - src/biointelligence/garmin/client.py
  - src/biointelligence/garmin/extractors.py
  - src/biointelligence/garmin/models.py
  - .env.example
  - .gitignore
  - .python-version
  - tests/__init__.py
  - tests/test_models.py
  - tests/test_extractors.py
autonomous: true
requirements:
  - DATA-01
  - DATA-02
  - DATA-03
  - DATA-04
  - DATA-05
  - DATA-06
  - DATA-08
user_setup:
  - service: garmin-connect
    why: "Garmin biometric data source -- personal account required"
    env_vars:
      - name: GARMIN_EMAIL
        source: "Your Garmin Connect login email"
      - name: GARMIN_PASSWORD
        source: "Your Garmin Connect login password"
    dashboard_config:
      - task: "Disable MFA if enabled (required for reliable token refresh per Issue #312)"
        location: "Garmin Connect -> Account Settings -> Security -> Two-Factor Authentication"
  - service: supabase
    why: "Database for storing daily biometric data"
    env_vars:
      - name: SUPABASE_URL
        source: "Supabase Dashboard -> Settings -> API -> Project URL"
      - name: SUPABASE_KEY
        source: "Supabase Dashboard -> Settings -> API -> anon/public key (or service_role key)"
    dashboard_config:
      - task: "Create a new Supabase project named 'biointelligence' in eu-central-1 (Frankfurt) region"
        location: "Supabase Dashboard -> New Project"

must_haves:
  truths:
    - "Project initializes with uv and all dependencies install correctly"
    - "Garmin client authenticates and persists OAuth tokens to disk"
    - "All 11 metric endpoints are called with per-category error isolation"
    - "Pydantic models normalize raw Garmin JSON into typed, validated fields"
    - "Completeness scoring detects missing critical fields and no-wear days"
  artifacts:
    - path: "pyproject.toml"
      provides: "Project metadata, dependencies, tool config"
      contains: "garminconnect"
    - path: "src/biointelligence/config.py"
      provides: "Typed configuration from .env"
      exports: ["Settings"]
    - path: "src/biointelligence/garmin/client.py"
      provides: "Garmin authentication with token persistence"
      exports: ["get_authenticated_client"]
    - path: "src/biointelligence/garmin/extractors.py"
      provides: "Per-category metric extraction with retry and error isolation"
      exports: ["extract_all_metrics"]
    - path: "src/biointelligence/garmin/models.py"
      provides: "Pydantic models for daily metrics, activities, completeness"
      exports: ["DailyMetrics", "Activity", "CompletenessResult", "assess_completeness"]
  key_links:
    - from: "src/biointelligence/garmin/extractors.py"
      to: "garminconnect.Garmin"
      via: "method calls with tenacity retry"
      pattern: "_fetch_with_retry.*client.*method_name"
    - from: "src/biointelligence/garmin/models.py"
      to: "src/biointelligence/garmin/extractors.py"
      via: "normalize raw dict output into typed Pydantic models"
      pattern: "DailyMetrics.*Optional"
    - from: "src/biointelligence/config.py"
      to: ".env"
      via: "pydantic-settings BaseSettings"
      pattern: "class Settings.*BaseSettings"
---

<objective>
Scaffold the BioIntelligence project and build the Garmin data extraction layer with authentication, per-category metric extraction, Pydantic normalization models, and completeness scoring.

Purpose: Establish the project foundation and the entire "extract + validate" half of the pipeline. Plan 02 builds the "store + orchestrate" half on top of this.
Output: Working project with Garmin client, 11-endpoint extractor, Pydantic models for DailyMetrics and Activity, completeness assessment, and unit tests.
</objective>

<execution_context>
@/Users/raphaellafeldt/.claude/get-shit-done/workflows/execute-plan.md
@/Users/raphaellafeldt/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-data-ingestion-and-storage/01-CONTEXT.md
@.planning/phases/01-data-ingestion-and-storage/01-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Project scaffold, config, logging, and Garmin auth client</name>
  <files>
    pyproject.toml,
    .python-version,
    .gitignore,
    .env.example,
    src/biointelligence/__init__.py,
    src/biointelligence/config.py,
    src/biointelligence/logging.py,
    src/biointelligence/garmin/__init__.py,
    src/biointelligence/garmin/client.py,
    tests/__init__.py,
    tests/test_client.py
  </files>
  <behavior>
    - Test: Settings loads from env vars (GARMIN_EMAIL, GARMIN_PASSWORD, SUPABASE_URL, SUPABASE_KEY, GARMIN_TOKEN_DIR, TARGET_TIMEZONE, LOG_JSON)
    - Test: Settings validates required fields are present, raises on missing
    - Test: get_authenticated_client loads tokens from existing token dir (mock garminconnect.Garmin)
    - Test: get_authenticated_client falls back to email/password login when no token dir exists (mock)
    - Test: After successful login, tokens are dumped to token dir with 0o700 permissions (mock)
    - Test: GarminConnectAuthenticationError is raised cleanly when credentials are invalid (mock)
  </behavior>
  <action>
    1. Initialize project with `uv init --lib --name biointelligence` in project root. Set Python 3.12+ in .python-version.

    2. Add dependencies to pyproject.toml:
       - Runtime: garminconnect, supabase, pydantic-settings, structlog, tenacity
       - Dev: ruff, pytest, pytest-mock, mypy
       Configure ruff in pyproject.toml (line-length=100, target Python 3.12, isort+format rules).
       Configure pytest (testpaths=["tests"], pythonpath=["src"]).

    3. Create .gitignore covering Python, .env, __pycache__, .venv, dist, .garminconnect/, *.egg-info.

    4. Create .env.example with all required env vars (empty values, comments explaining each).

    5. Create src/biointelligence/config.py:
       - Settings class extending BaseSettings with SettingsConfigDict(env_file=".env")
       - Fields: garmin_email (str), garmin_password (str), garmin_token_dir (str, default "~/.garminconnect"), supabase_url (str), supabase_key (str), target_timezone (str, default "Europe/Berlin"), log_json (bool, default False)
       - Export a module-level `get_settings()` function using lru_cache for singleton behavior

    6. Create src/biointelligence/logging.py:
       - configure_logging(json_output: bool = False) function using structlog
       - Processors: merge_contextvars, add_log_level, TimeStamper(fmt="iso"), StackInfoRenderer, format_exc_info
       - JSON renderer when json_output=True, ConsoleRenderer otherwise
       - PrintLoggerFactory(file=sys.stderr)

    7. Create src/biointelligence/garmin/client.py:
       - get_authenticated_client(settings: Settings) -> Garmin function
       - Token persistence: if token_dir exists, call Garmin().login(token_dir). Otherwise login with email/password, then garth.dump(token_dir) and chmod 0o700.
       - Handle GarminConnectAuthenticationError with structured log message
       - Import from garminconnect: Garmin, GarminConnectAuthenticationError, GarminConnectConnectionError, GarminConnectTooManyRequestsError

    8. Run `uv sync` to install all dependencies. Run tests with `uv run pytest tests/ -x`.
  </action>
  <verify>
    <automated>cd /Users/raphaellafeldt/Git/garmin-data && uv run pytest tests/test_client.py -x -v</automated>
  </verify>
  <done>
    - pyproject.toml exists with all dependencies listed
    - `uv sync` completes without errors
    - Settings loads from environment variables and validates required fields
    - Garmin client authenticates via token persistence or email/password fallback (tested with mocks)
    - structlog logging is configured for dev (console) and production (JSON)
    - All tests pass
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Metric extractors, Pydantic models, and completeness scoring</name>
  <files>
    src/biointelligence/garmin/extractors.py,
    src/biointelligence/garmin/models.py,
    tests/test_extractors.py,
    tests/test_models.py,
    tests/fixtures/garmin_responses.json
  </files>
  <behavior>
    - Test: extract_all_metrics calls all 11 endpoints (stats, heart_rates, sleep, hrv, body_battery, stress, spo2, respiration, training_status, training_readiness, max_metrics) plus activities
    - Test: When one endpoint fails, others still succeed (per-category error isolation)
    - Test: Retry decorator triggers on GarminConnectConnectionError and GarminConnectTooManyRequestsError
    - Test: Failed endpoints return None in the raw dict, not raise
    - Test: DailyMetrics model accepts full Garmin-like data and produces typed fields
    - Test: DailyMetrics model accepts partial data (many None fields) without validation error
    - Test: Activity model normalizes activity summary data
    - Test: normalize_daily_metrics extracts correct fields from raw Garmin response dicts (sleep_seconds from sleep data, hrv_overnight_avg from HRV data, etc.)
    - Test: normalize_activities handles empty activity list gracefully
    - Test: assess_completeness returns score=1.0 when all critical fields present
    - Test: assess_completeness returns missing_critical list when fields are None
    - Test: assess_completeness detects no-wear day when ALL critical fields are None (is_no_wear=True)
    - Test: completeness_score is set on the DailyMetrics record after assessment
  </behavior>
  <action>
    1. Create tests/fixtures/garmin_responses.json with realistic mock data for each of the 11 endpoints plus activities. Use realistic Garmin field names (research Pattern 5 for field names). Include a "partial" fixture set with some endpoints returning None.

    2. Create src/biointelligence/garmin/models.py:
       - DailyMetrics(BaseModel): all fields from research Pattern 5 (date, sleep fields, HRV fields, Body Battery fields, HR fields, stress fields, training fields, general fields, raw_data JSONB, is_no_wear, completeness_score)
       - Activity(BaseModel): date, activity_type, name, duration_seconds, distance_meters, avg_hr, max_hr, calories, training_effect_aerobic, training_effect_anaerobic, vo2_max_activity, raw_data
       - CompletenessResult(BaseModel): score (float), critical_present (int), critical_total (int), missing_critical (list[str]), is_no_wear (bool)
       - normalize_daily_metrics(raw_data: dict, target_date: date) -> DailyMetrics function that maps raw Garmin JSON keys to model fields. Store full raw_data dict. Handle missing keys gracefully (default None).
       - normalize_activities(raw_activities: list, target_date: date) -> list[Activity] function
       - assess_completeness(record: DailyMetrics) -> CompletenessResult function with critical fields: total_sleep_seconds, hrv_overnight_avg, body_battery_morning, resting_hr, avg_stress_level, steps. Score = non-null / total fields count.

    3. Create src/biointelligence/garmin/extractors.py:
       - _fetch_with_retry(client, method_name, *args) with tenacity decorator: stop_after_attempt(3), wait_exponential(multiplier=1, min=2, max=60), retry on GarminConnectConnectionError and GarminConnectTooManyRequestsError
       - extract_all_metrics(client: Garmin, target_date: date) -> dict: calls all 11 endpoints via _fetch_with_retry, each in try/except. Failed endpoints log warning and set None. Activities use get_activities_by_date(date_str, date_str).
       - Endpoints map: stats->get_stats, heart_rates->get_heart_rates, sleep->get_sleep_data, hrv->get_hrv_data, body_battery->get_body_battery, stress->get_stress_data, spo2->get_spo2_data, respiration->get_respiration_data, training_status->get_training_status, training_readiness->get_training_readiness, max_metrics->get_max_metrics

    4. Update src/biointelligence/garmin/__init__.py to export: get_authenticated_client, extract_all_metrics, DailyMetrics, Activity, CompletenessResult, normalize_daily_metrics, normalize_activities, assess_completeness.

    5. Run full test suite: `uv run pytest tests/ -x -v`.
  </action>
  <verify>
    <automated>cd /Users/raphaellafeldt/Git/garmin-data && uv run pytest tests/ -x -v</automated>
  </verify>
  <done>
    - All 11 Garmin endpoints are called with retry logic and per-category error isolation
    - DailyMetrics and Activity Pydantic models validate and normalize raw Garmin data
    - normalize_daily_metrics maps raw JSON keys to typed model fields
    - assess_completeness scores data quality and detects no-wear days
    - All tests pass including fixtures with partial/missing data
    - Exports are clean from garmin package __init__.py
  </done>
</task>

</tasks>

<verification>
All Phase 1 extraction/validation capabilities verified:
1. Project structure follows research-recommended layout (src/biointelligence/garmin/)
2. Config loads from .env via pydantic-settings
3. Garmin auth with token persistence works (mocked)
4. 11 metric endpoints extracted with retry and error isolation (mocked)
5. Pydantic models normalize raw data to typed fields
6. Completeness scoring detects gaps and no-wear days
7. All unit tests pass: `uv run pytest tests/ -x -v`
</verification>

<success_criteria>
- `uv sync` installs all dependencies without errors
- `uv run pytest tests/ -x -v` passes all tests
- `uv run ruff check src/ tests/` reports no issues
- DailyMetrics model handles both complete and partial Garmin data
- Completeness scorer correctly identifies missing critical fields
- Garmin client handles token persistence path (tested with mocks)
</success_criteria>

<output>
After completion, create `.planning/phases/01-data-ingestion-and-storage/01-01-SUMMARY.md`
</output>
